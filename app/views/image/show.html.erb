<%= provide(:title, "Upload Image") %>
<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <div class="thumbnail">
      <a href="<%= @image.document.url(:largest) %>"><%= image_tag(@image.document.url(:medium), class: "img-responsive img-rounded center-block") %></a>
      <div class="caption">
        <h5 class="text-muted">Uploaded by <%= User.find_by(id: @image.user_id).email %></h5>
      </div>
    </div>
  </div>
</div>
