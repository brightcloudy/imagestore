<%= provide(:title, "Upload Image") %>
<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <%= simple_form_for @image, url: image_index_path do |f| %>
      <%= f.input :document, :required => :true %>
      <%= f.button :submit %>
    <% end %>
  </div>
</div>
